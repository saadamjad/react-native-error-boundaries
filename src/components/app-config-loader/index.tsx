/**
 * eslint-disable no-nested-ternary
 *
 * @format
 */

import React from 'react';
import { Routes } from '../../routes/index-routes';

const AppConfigLoaderFunction: React.FC = () => {
	return (	<Routes />
	);
};

export default AppConfigLoaderFunction;
